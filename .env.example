# --- Base de données MariaDB ---
# Sur le mutualisé : socket UNIX (chemin donné par l'hébergeur, souvent /var/run/mysqld/mysqld.sock)
DB_SOCKET=/var/run/mysqld/mysqld.sock
# Ou en TCP si pas de socket : DB_HOST=127.0.0.1 / DB_PORT=3306 (le socket a priorité s'il est défini)
DB_HOST=
DB_PORT=3306
DB_NAME=firemap
DB_USER=firemap
DB_PASS=changeme

# --- Serveur ---
PORT=3000
# URL publique (pour les liens de partage et le QR) — ex: https://entraide.mondomaine.fr
BASE_URL=http://localhost:3000
# Email de contact (obligatoire pour le push VAPID + mentions légales)
CONTACT_EMAIL=h2co3_official@protonmail.com

# Clé NASA FIRMS (gratuite : https://firms.modaps.eosdis.nasa.gov/api/map_key/)
# → détections de feu vectorielles horodatées. Sans clé : repli raster GIBS.
FIRMS_MAP_KEY=

# --- Territoire (LE bloc à adapter pour un fork — cf. FORKING.md) ---
# Vue initiale de la carte : latitude, longitude, zoom
REGION_CENTER=44.8,-0.9,9
# Zone navigable de la carte : sud, ouest, nord, est
REGION_BOUNDS=40.5,-6.5,51.8,10.5
# Bbox des détections satellites (FIRMS) et du géocodage : ouest, sud, est, nord
GEO_BBOX=-2.0,43.3,1.5,46.2
# Sources d'import des points officiels : « url|type,url|type » — ou « none ».
# Le parseur attend le gabarit HTML d'alertesfeux.fr (cf. importer.js).
# IMPORT_SOURCES=

# --- Alertes par e-mail (SMTP de l'hébergeur, ex. o2switch) ---
# Sans SMTP_HOST : les e-mails sont simulés dans logs/mail.log (mode dev)
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=1
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Entraide Feu <alerte@votredomaine.fr>"

# --- HTTPS local optionnel (dev mobile, certificats mkcert) ---
# Laisser vide en production : le TLS est géré par le reverse proxy de l'hébergeur.
# HTTPS_KEY=certs/dev-key.pem
# HTTPS_CERT=certs/dev-cert.pem

# --- Optionnel : générés automatiquement dans data/ au premier démarrage si absents ---
# SECRET=            (clé HMAC des cookies)
# ADMIN_KEY=         (clé d'accès à /admin.html)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
